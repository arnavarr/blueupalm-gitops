# flux/clusters/bc-workload/infrastructure/cloud-controller-manager/kustomization.yaml
# Kustomize resource list for the GCP Cloud Controller Manager layer.
# This is a Kustomize kustomization.yaml (NOT a Flux Kustomization CRD).
# The Flux Kustomization object for this layer is defined in flux-system/kustomizations.yaml.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cloud-controller-manager.yaml
