# flux/clusters/bc-workload/networking/traefik/helmrelease.yaml
# BlueUPALM — Traefik Ingress Controller
#
# Punto de entrada ÚNICO para tráfico externo.
# Rutas expuestas (sobre navarro-bores.com):
#   blueupalm.navarro-bores.com/         → frontend React
#   blueupalm.navarro-bores.com/api/edge/ → edge-security (solo /issue y /health)
#   auth.navarro-bores.com/              → Keycloak
#   hubble.navarro-bores.com/            → Hubble UI (Cilium observabilidad)
#   grafana.navarro-bores.com/           → Grafana
#
# NUNCA expuesto: NATS :8222, OPA :8181, SPIRE, Trino, PostgreSQL, /verify endpoint

---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: traefik
  namespace: networking
spec:
  interval: 24h
  url: https://traefik.github.io/charts

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: networking
spec:
  interval: 10m
  chart:
    spec:
      chart: traefik
      version: "27.*"    # Traefik v3.x
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: networking

  values:
    # Usar la IP estática regional reservada por Terraform (blueupalm-ingress-ip)
    # La anotación networking.gke.io/load-balancer-ip instruye al GCP CCM a usar
    # la IP reservada en lugar de asignar una efímera nueva.
    # CRÍTICO: el nombre del recurso GCP debe coincidir con terraform/main.tf
    service:
      type: LoadBalancer
      annotations:
        # GCP CCM: pedir la IP estática por nombre de recurso GCP
        networking.gke.io/load-balancer-ip: "blueupalm-ingress-ip"
        # Solo tráfico desde cualquier origen (sin restricción de origen)
        networking.gke.io/load-balancer-ip-source-ranges: "0.0.0.0/0"

    # TLS: usar el certificado wildcard emitido por cert-manager
    ports:
      web:
        redirectTo:
          port: websecure   # Forzar HTTPS
      websecure:
        tls:
          enabled: true
          options: "default"

    # Acceso al dashboard solo desde dentro del cluster
    ingressRoute:
      dashboard:
        enabled: false

    # Prometheus metrics
    metrics:
      prometheus:
        entryPoint: metrics
        addServicesLabels: true
        addEntryPointsLabels: true
        serviceMonitor:
          enabled: false  # Requiere CRDs monitoring.coreos.com — activar tras kube-prometheus-stack

    # Pod Security
    podSecurityContext:
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault

    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop: ["ALL"]
        add: ["NET_BIND_SERVICE"]

